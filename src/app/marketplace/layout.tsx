/**
 * Marketplace Layout - Immersive AOS Terminal Mode
 * NO traditional navigation - handled by AgentHUD OS_NAV
 */
export default function MarketplaceLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
